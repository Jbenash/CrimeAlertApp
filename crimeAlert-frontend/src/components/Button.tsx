// Button Component
// Reusable button component with multiple variants
//
// TODO: Implement the following:
// - Props: title, onPress, variant (primary/secondary/danger), disabled, loading
// - Variants with different colors (primary=blue, secondary=gray, danger=red)
// - TouchableOpacity with custom styling
// - Loading spinner when loading=true
// - Disabled state with reduced opacity
// - Support for custom styles via style prop
// - Ripple effect on Android
// - Accessibility labels
